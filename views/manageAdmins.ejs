<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Events</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
      padding: 0;
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 0 auto;
    }

    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: center;
    }

    th {
      background-color: #f4f4f4;
      font-weight: bold;
    }

    tr:nth-child(even) {
      background-color: #f9f9f9;
    }

    tr:hover {
      background-color: #f1f1f1;
    }

    button {
      padding: 5px 10px;
      font-size: 14px;
      cursor: pointer;
    }

    form {
      display: inline;
    }
  </style>
</head>
<body>
<h1>Manage Admin Users</h1>
<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Date of Birth</th>
      <th>Gender</th>
      <th>Phone Number</th>
      <th>Email Address</th>
      <th>City</th>
      <th>State</th>
      <th>Preferred Contact Method</th>
      <th>Created By</th>
      <th>Created Date</th>
    </tr>
  </thead>
  <tbody>
    <% admins.forEach(admins => { %>
      <tr>
        <td><%= admins.username %></td>
        <td><%= admins.first_name %></td>
        <td><%= admins.last_name %></td>
        <td><%= admins.date_of_birth %></td>
        <td><%= admins.gender %></td>
        <td><%= admins.phone_number %></td>
        <td><%= admins.email_address %></td>
        <td><%= admins.city %></td>
        <td><%= admins.state %></td>
        <td><%= admins.preferred_contact_method %></td>
        <td><%= admins.created_by %></td>
        <td><%= admins.created_date %></td>
        <td>
          <form action="/adminEditAdmin/<%= admin.contact_id %>" method="GET">
            <button type="submit">Edit</button>
          </form>
          <form action="/adminDeleteAdmin/<%= admin.contact_id %>" method="POST">
            <button type="submit" onclick="return confirm('Are you sure you want to delete this admin user?');">Delete</button>
          </form>
        </td>
      </tr>
    <% }); %>
  </tbody>
</table>
</body>
</html>