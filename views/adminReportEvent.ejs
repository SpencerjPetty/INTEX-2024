<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View/Report Event Results</title>
  <link rel="stylesheet" href="/css/adminReport.css">
</head>
<body>
  <a href="/admin/manageEvents" class="back-home-btn">Back to Manage Page</a>
  <h1>View/Report Event Results</h1>

  <form action="/admin/reportEvent/<%= event.event_id %>" method="POST">
    <input type="hidden" name="event_id" value="<%= event.event_id %>">

    <!-- Organization Information -->
    <div class="info-block">
      <label for="organization">Organization:<span class="required">*</span></label>
      <input type="text" id="organization" name="organization" value="<%= event.org_name %>" readonly><br>
      
      <label for="estimated_attendance">Estimated Attendance:<span class="required">*</span></label>
      <input type="number" id="estimated_attendance" name="estimated_attendance" value="<%= event.total_attendance_estimate %>" readonly><br>

      <label for="planned_duration">Planned Duration (hours):<span class="required">*</span></label>
      <input type="number" id="planned_duration" name="planned_duration" value="<%= event.planned_hour_duration %>" readonly><br>
    </div>

    <!-- Displaying Existing Results if Available -->
    <label for="event_date">Event Date:<span class="required">*</span></label>
    <input type="datetime-local" id="event_date" name="event_date" value="<%= event.event_date ? event.event_date.toISOString().slice(0, 16) : '' %>" required><br>

    <label for="num_participants">Actual Number of Participants:<span class="required">*</span></label>
    <input type="number" id="num_participants" name="num_participants" value="<%= event.num_participants || '' %>" min="0" required><br>

    <label for="event_duration_hours">Actual Event Duration (Hours):<span class="required">*</span></label>
    <input type="number" id="event_duration_hours" name="event_duration_hours" value="<%= event.event_duration_hours || '' %>" min="0" step="0.1" required><br>

    <label for="pockets_produced"># Pockets Produced:<span class="required">*</span></label>
    <input type="number" id="pockets_produced" name="pockets_produced" value="<%= event.pockets_produced || '' %>" min="0" required><br>

    <label for="collars_produced"># Collars Produced:<span class="required">*</span></label>
    <input type="number" id="collars_produced" name="collars_produced" value="<%= event.collars_produced || '' %>" min="0" required><br>

    <label for="envelopes_produced"># Envelopes Produced:<span class="required">*</span></label>
    <input type="number" id="envelopes_produced" name="envelopes_produced" value="<%= event.envelopes_produced || '' %>" min="0" required><br>

    <label for="vests_produced"># Vests Produced:<span class="required">*</span></label>
    <input type="number" id="vests_produced" name="vests_produced" value="<%= event.vests_produced || '' %>" min="0" required><br>

    <label for="total_products_completed"># Total Products Completed:<span class="required">*</span></label>
    <input type="number" id="total_products_completed" name="total_products_completed" value="<%= event.total_products_completed || '' %>" min="0" required><br>

    <label for="pockets_max_possible"># Max Pockets Produced with Materials Brought:</label>
    <input type="number" id="pockets_max_possible" name="pockets_max_possible" value="<%= event.pockets_max_possible || '' %>" min="0" required><br>

    <label for="collars_max_possible"># Max Collars Produced with Materials Brought:</label>
    <input type="number" id="collars_max_possible" name="collars_max_possible" value="<%= event.collars_max_possible || '' %>" min="0" required><br>

    <label for="envelopes_max_possible"># Max Envelopes Produced with Materials Brought:</label>
    <input type="number" id="envelopes_max_possible" name="envelopes_max_possible" value="<%= event.envelopes_max_possible || '' %>" min="0" required><br>

    <label for="vests_max_possible"># Max Vests Produced with Materials Brought:</label>
    <input type="number" id="vests_max_possiblev" name="vests_max_possible" value="<%= event.vests_max_possible || '' %>" min="0" required><br>

    <button type="submit">Save Changes</button>
  </form>
</body>
</html>
